/* EasySSH Core FFI - Auto-generated by cbindgen */

#ifndef EASYSSH_CORE_H
#define EASYSSH_CORE_H

/* Warning: this file is auto-generated. Do not modify. */

#include <stdarg.h>
#include <stdbool.h>
#include <stdint.h>
#include <stdlib.h>

/**
 * Opaque handle to AppState
 */
typedef struct easyssh_EasySSHAppState easyssh_EasySSHAppState;

/**
 * Add a new group
 *
 * # Safety
 * `json_config` must be valid null-terminated UTF-8 JSON.
 * Returns 0 on success, -1 on error.
 */
int easyssh_add_group(struct easyssh_EasySSHAppState *handle, const char *json_config);

/**
 * Add a new server
 *
 * # Safety
 * `json_config` must be a valid null-terminated UTF-8 string containing NewServer JSON.
 * Returns 0 on success, -1 on error.
 */
int easyssh_add_server(struct easyssh_EasySSHAppState *handle, const char *json_config);

/**
 * Connect to server using native terminal
 *
 * # Safety
 * `server_id` must be a valid null-terminated UTF-8 string.
 * Returns 0 on success, -1 on error.
 */
int easyssh_connect_native(struct easyssh_EasySSHAppState *handle, const char *server_id);

/**
 * Delete a server by ID
 *
 * # Safety
 * `server_id` must be a valid null-terminated UTF-8 string.
 * Returns 0 on success, -1 on error.
 */
int easyssh_delete_server(struct easyssh_EasySSHAppState *handle, const char *server_id);

/**
 * Destroy the EasySSH app state
 *
 * # Safety
 * Must be called with a valid handle returned from `easyssh_init`.
 * Handle must not be used after this call.
 */
void easyssh_destroy(struct easyssh_EasySSHAppState *handle);

/**
 * Free a string returned by FFI functions
 *
 * # Safety
 * Must be called with a string returned from easyssh_ functions.
 * String must not be used after this call.
 */
void easyssh_free_string(char *s);

/**
 * Get edition info as JSON string
 *
 * # Safety
 * Caller must free the returned string with `easyssh_free_string`.
 */
char *easyssh_get_edition(struct easyssh_EasySSHAppState *handle);

/**
 * Get all groups as JSON array
 *
 * # Safety
 * Caller must free the returned string with `easyssh_free_string`.
 */
char *easyssh_get_groups(struct easyssh_EasySSHAppState *handle);

/**
 * Get all servers as JSON array
 *
 * # Safety
 * Caller must free the returned string with `easyssh_free_string`.
 */
char *easyssh_get_servers(struct easyssh_EasySSHAppState *handle);

/**
 * Initialize the EasySSH core library
 *
 * # Safety
 * Must be called before any other FFI function.
 * Returns a handle that must be freed with `easyssh_destroy`.
 */
struct easyssh_EasySSHAppState *easyssh_init(void);

/**
 * Get the last error message
 *
 * # Safety
 * Returns a null-terminated string. Caller must not free.
 * String is only valid until next FFI call.
 */
const char *easyssh_last_error(void);

/**
 * Get the library version
 *
 * # Safety
 * Returns a null-terminated string. Caller must not free.
 */
const char *easyssh_version(void);

#endif  /* EASYSSH_CORE_H */
